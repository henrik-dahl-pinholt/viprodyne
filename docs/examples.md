# Examples

The repository includes a contact-threshold recovery example:

- `examples/contact_threshold_profile.ipynb`
- `examples/contact_threshold_profile.py`

The notebook is committed with rendered output so it can be inspected directly
on GitHub.

Run the script version from the repository root:

```bash
python examples/contact_threshold_profile.py
```
